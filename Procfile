# Uncomment the following line if you aren't using pow
# web: bundle exec unicorn -c ./config/unicorn/production.rb
mailer: bundle exec sidekiq
mailcatcher: bundle exec mailcatcher -f

# Sync output buffers in development
$stdout.sync = true
